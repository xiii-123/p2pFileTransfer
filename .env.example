# P2P 文件传输系统 - 环境变量配置示例
# P2P File Transfer System - Environment Variables Example
#
# 使用方法 / Usage:
# 1. 复制此文件为 .env: cp .env.example .env
# 2. 修改配置项
# 3. 在启动服务前加载: source .env (Linux/macOS) 或使用工具加载
#
# 注意 / Note:
# - Windows 用户可以使用 PowerShell 环境变量或直接在命令行设置
# - 环境变量优先级高于配置文件
# - 环境变量会覆盖配置文件中的对应值

# ==================== 网络配置 / Network Configuration ====================

# 监听端口 (0 = 自动分配随机端口)
# Listen port (0 = auto-assign random port)
# P2P_PORT=0

# 是否使用不安全的连接（仅用于开发测试）
# Use insecure connection (development/testing only)
# P2P_INSECURE=false

# 随机种子（用于确定性密钥生成）
# Random seed for deterministic key generation
# P2P_SEED=0

# Bootstrap 节点地址（用于初始网络连接）
# Bootstrap peer addresses (comma-separated)
# 格式示例 / Example format:
# P2P_BOOTSTRAP_PEERS=/ip4/127.0.0.1/tcp/8001/p2p/QmPeerID123,/ip4/192.168.1.100/tcp/8001/p2p/QmPeerID456

# 协议前缀
# Protocol prefix
# P2P_PROTOCOL_PREFIX=/p2p-file-transfer

# DHT 命名空间
# DHT namespace
# P2P_NAMESPACE=p2p-file-transfer

# ==================== 存储配置 / Storage Configuration ====================

# Chunk 存储路径
# Chunk storage path
# P2P_CHUNK_PATH=files

# Merkle 树块大小（字节）
# Merkle tree block size in bytes
# P2P_BLOCK_SIZE=262144

# 缓冲区数量
# Buffer number
# P2P_BUFFER_NUMBER=16

# ==================== 性能配置 / Performance Configuration ====================

# 最大重试次数
# Max retry attempts
# P2P_MAX_RETRIES=3

# 最大并发下载数
# Max concurrent downloads
# P2P_MAX_CONCURRENCY=16

# 请求超时（秒）
# Request timeout in seconds
# P2P_REQUEST_TIMEOUT=5

# 数据传输超时（秒）
# Data transfer timeout in seconds
# P2P_DATA_TIMEOUT=30

# DHT 操作超时（秒）
# DHT operation timeout in seconds
# P2P_DHT_TIMEOUT=10

# ==================== 日志配置 / Logging Configuration ====================

# 日志级别 (debug, info, warn, error)
# Log level (debug, info, warn, error)
# P2P_LOG_LEVEL=info

# 日志格式 (json, text)
# Log format (json, text)
# P2P_LOG_FORMAT=text

# ==================== 反吸血虫配置 / Anti-Leecher Configuration ====================

# 是否启用反吸血虫机制
# Enable anti-leecher mechanism
# P2P_ANTI_LEECHER_ENABLED=true

# 最小成功率阈值（0.0-1.0）
# Minimum success rate threshold (0.0-1.0)
# P2P_MIN_SUCCESS_RATE=0.5

# 黑名单前的最小请求数
# Minimum requests before blacklisting
# P2P_MIN_REQUESTS=10

# ==================== 使用示例 / Usage Examples ====================

# 示例 1: 开发环境（禁用安全性，启用调试日志）
# Development environment:
# P2P_INSECURE=true
# P2P_LOG_LEVEL=debug
# P2P_LOG_FORMAT=text

# 示例 2: 生产环境（启用所有安全特性）
# Production environment:
# P2P_INSECURE=false
# P2P_ANTI_LEECHER_ENABLED=true
# P2P_LOG_LEVEL=info

# 示例 3: 高性能配置
# High performance configuration:
# P2P_MAX_CONCURRENCY=32
# P2P_MAX_RETRIES=5
# P2P_BUFFER_NUMBER=32

# 示例 4: 低带宽环境
# Low bandwidth environment:
# P2P_MAX_CONCURRENCY=8
# P2P_REQUEST_TIMEOUT=10
# P2P_DATA_TIMEOUT=60
